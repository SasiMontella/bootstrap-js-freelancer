


function bookService(event) {
    event.preventDefault();

    let hoursSelected = document.getElementById("hours").value;
    hoursSelected = parseFloat(hoursSelected);
    //console.log(hoursSelected)
    let backEndDev = document.getElementById("backEnd").innerText;
    //console.log(backEndDev)
}